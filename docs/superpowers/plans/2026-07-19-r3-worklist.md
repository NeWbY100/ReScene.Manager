# Plan r3 worklist (codex r2b findings -> coherent region rewrites)

Method: REWRITE each flagged region completely (patches left contradictions — the r2b lesson).
All 16 with locations from `docs/superpowers/plans/2026-07-19-plan-review-codex-r2b.log`:

1. Task 1: junction test via unprivileged NTFS JUNCTION (`cmd /c mklink /J` helper — no admin
   needed, removes skip entirely); define `ResolveAncestorChain` in the impl block; alias-through-
   parent + case tests.
2. Task 2: chain key = DIRECTORY-qualified base name (same basename in different dirs ≠ same
   chain); first-volume rules add `.part1.rar`/`.part001.rar` variants ("lowest partN present");
   tests for part1/part01/part001 + cross-dir same basename.
3. Move `ProofRarFacts` + production `RarProofInspector.Inspect(path)` into ReScene.Lib (public —
   lib owns RAR parsing; App.Core consumes public API). App.Core.Tests use the injectable seam
   with fact literals; the production-parser fixture test lives in ReScene.Tests beside
   RarFixtures. Update Tasks 5/7 files+interfaces accordingly.
4. Task 3: DELETE the Sample/ line from writer-only trees (sets+nfo+one excluded SFV only).
   Task 9: explicit step enabling FullPipelineGoldenTests + regen command WITH `--vobsub-srr`,
   WITHOUT `--no-srs`; define nested-SRR app-name normalization reuse.
5. DONE (excerpt now 1564 lines: get_start_rar_files 578-660 + create_srr_for_subs complete).
6. Task 7 pass 10: adjacency move covers `.srr` AND proof `.rar` (excerpt tail L1243); Task 9
   gets an executable reorder step + mixed-tree full-order test asserting the COMPLETE list.
9. Task 2 clause 1: rewrite the whole numbered contract list; kill every residual "error on
   zero/zero" phrasing (grep the task body for "error" near zero/stored).
10. Task 1: add `CanonicalizeLogicalName` implementation to the code block + its own test facts
    (rooted/empty/`.`/`..`/backslash normalization).
12. Task 4: rewrite the ENTIRE implementation snippet to the TraversalResult shape (issues list,
    RootFailed, ct per directory, `FileAttributes.ReparsePoint` dirs not descended) matching the
    already-updated interface text.
13. Task 7 fixture rows: image `grp-movie.jpg` beside sfv `grp-movie.sfv` (true prefix match);
    strip_zeros row: nfo `00-grp.nfo` + image `grp-front.jpg` >100KB (similarity via stripped
    stem, image itself NOT 00-prefixed).
14. Task 8 files list gains the composition-root/DI registration (App/Program + both VM sites);
    Task 10 files list drops it (markup/bindings only) — remove the contradicting line.
15. Task 8 BEHAVIOR text (not only tests): every-input-change invalidation (file/blank/
    nonexistent), current-generation cleanup, ALL warnings ordered.
16. Task 10 steps: explicit RED run → markup → GREEN run for the binding test; add
    `CreateSRRWizardBodyBindingTests` too. Task 11: bridge-CLICK Browse folder on BOTH surfaces.
17. Execution Regime: review-package invoked as
    `bash "$HOME/.claude/plugins/cache/claude-plugins-official/superpowers/6.1.1/skills/subagent-driven-development/scripts/review-package" BASE HEAD`
    (verified to exist 2026-07-19); fallback = manual `git log --oneline && git diff -U10` per
    the sdd skill. No `<user>` placeholders.
18. Tasks 1-3 step lists: insert "Step 0: commit compiling stubs (NotImplementedException)" and
    change RED expectations from compile errors to named assertion failures.

After r3: relaunch codex re-review with the same self-contained prompt style (list items 1-18
inline, do-not-read-logs). On APPROVE → execution per standing approval.
