<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:ReScene.Manager.Controls"
             x:Class="ReScene.Manager.Views.Wizards.ReconstructWizardBody"
             x:CompileBindings="False">
  <!-- DataContext is the wizard's dedicated ReconstructorViewModel. Step visibility is driven by the
       hosting Window's WizardViewModel.CurrentStepIndex (reached with $parent[Window]). -->
  <Grid MinHeight="150">

    <!-- Step 0: import SRR -->
    <StackPanel IsVisible="{Binding $parent[Window].DataContext.CurrentStepIndex,
                                    Converter={StaticResource IndexEqualsConverter}, ConverterParameter=0}">
      <TextBlock Text="Import the release's SRR — compression settings and file list are configured automatically."
                 Foreground="{DynamicResource ForegroundSecondary}"
                 FontSize="{DynamicResource FontSizeCaption}"
                 TextWrapping="Wrap" Margin="0,0,0,12" />
      <!-- The Import button and a live outcome line share one row, which is the whole reason the
           row exists. The custom-packer warning below toggles IsVisible and therefore announces
           nothing (4.1.3) — the same defect the Advanced tab fixed by mirroring the text into a
           live line in its LOG HEADER, deliberately away from the banner. This step has no log
           header, which is what was recorded as "no neutral home" and deferred.
           This row IS that home, and it is the established pattern rather than a new one: it is
           what SRSReconstructorView does with ResultStatus beside Save-log, and what the Advanced
           Reconstructor does with CustomPackerStatus. Putting the live line in its own block was
           MEASURED to cost 31 DIPs of permanent empty space — an empty TextBlock still reserves a
           line box — whereas here the row's height is the button's and an empty line costs nothing.
           TextTrimming keeps a long warning from stretching the row; the announced value is the
           full Text, never the rendered glyphs. -->
      <DockPanel>
        <Button DockPanel.Dock="Left" Content="Import from SRR..." Command="{Binding ImportSRRCommand}"
                Classes="primary" Padding="14,6"
                ToolTip.Tip="Pick the release's .srr file. Compression, version, dictionary and timestamps are configured automatically." />
        <!-- Always in the tree; empty text renders nothing. No AutomationProperties.Name: the
             announced name IS the text. -->
        <TextBlock x:Name="CustomPackerStatus" Text="{Binding CustomPackerWarning}"
                   AutomationProperties.LiveSetting="Polite"
                   Foreground="{DynamicResource ForegroundSecondary}" FontSize="{DynamicResource FontSizeCaption}"
                   TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="8,0" />
      </DockPanel>

      <!-- Details of the imported SRR -->
      <Border IsVisible="{Binding HasImportedSRR}"
              BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="1"
              Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
              CornerRadius="3" Margin="0,12,0,0" Padding="12,10">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>

          <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                     Text="{Binding ImportedSRRName}" FontWeight="SemiBold"
                     TextWrapping="Wrap" Margin="0,0,0,8" />

          <TextBlock Grid.Row="1" Grid.Column="0" Text="Created with"
                     Foreground="{DynamicResource ForegroundSecondary}" Margin="0,0,16,4" />
          <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ImportedSRRAppName}"
                     TextWrapping="Wrap" Margin="0,0,0,4" />

          <TextBlock Grid.Row="2" Grid.Column="0" Text="RAR volumes"
                     Foreground="{DynamicResource ForegroundSecondary}" Margin="0,0,16,4" />
          <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding ImportedRARVolumeText}" Margin="0,0,0,4" />

          <TextBlock Grid.Row="3" Grid.Column="0" Text="Archived files"
                     Foreground="{DynamicResource ForegroundSecondary}" Margin="0,0,16,4" />
          <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ImportedArchivedFilesText}" Margin="0,0,0,4" />

          <TextBlock Grid.Row="4" Grid.Column="0" Text="Compression"
                     Foreground="{DynamicResource ForegroundSecondary}" Margin="0,0,16,4" />
          <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding ImportedCompressionText}" Margin="0,0,0,4" />

          <TextBlock Grid.Row="5" Grid.Column="0" Text="Stored files"
                     Foreground="{DynamicResource ForegroundSecondary}" Margin="0,0,16,0" VerticalAlignment="Top" />
          <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding ImportedStoredFilesText}" TextWrapping="Wrap" />
        </Grid>
      </Border>

      <Border IsVisible="{Binding HasCustomPackerWarning}"
              Background="#3DE0A030" BorderBrush="#E0A030" BorderThickness="1"
              CornerRadius="3" Margin="0,8,0,0" Padding="8,5">
        <!-- This banner stays announcement-free, exactly as SRSReconstructorView's ResultBanner and
             the Advanced Reconstructor's own copy of THIS banner do: it toggles IsVisible, so it is
             not realized when its text arrives and there is no empty-to-text transition for an
             assistive technology to notice. The announcement rides the always-in-tree live line
             beside the Import button above.
             FontSizeBody on purpose: mirrors the Advanced tab's CustomPackerWarning, which is
             v1.9 parity (ReconstructorView.xaml:74 pinned Body) — not a stray root-pin-era 14. -->
        <TextBlock Text="{Binding CustomPackerWarning}" Foreground="#FFD080"
                   FontSize="{DynamicResource FontSizeBody}" TextWrapping="Wrap" />
      </Border>
    </StackPanel>

    <!-- Step 1: files & folders (WinRAR versions, release files, output, verification).
         Scrollable: this step is dense and must not clip on small windows. AllowAutoHide=False:
         the Fluent overlay scrollbar (persistently expanded on Linux) drew ON TOP of the
         right-edge Browse buttons; reserving layout space keeps controls clear of the bar. -->
    <ScrollViewer VerticalScrollBarVisibility="Auto" ScrollViewer.AllowAutoHide="False"
                  IsVisible="{Binding $parent[Window].DataContext.CurrentStepIndex,
                                      Converter={StaticResource IndexEqualsConverter}, ConverterParameter=1}">
      <StackPanel>
      <TextBlock Text="ReScene rebuilds the original RAR archives from these — here's what to pick for each:"
                 Foreground="{DynamicResource ForegroundSecondary}"
                 FontSize="{DynamicResource FontSizeCaption}"
                 TextWrapping="Wrap" Margin="0,0,0,12" />

      <TextBlock x:Name="WinRarFolderLabel" Text="WinRAR versions folder" FontWeight="SemiBold" Margin="0,0,0,2" />
      <TextBlock Text="The folder holding your different WinRAR versions, each in its own subfolder. ReScene uses the version that matches the release so the rebuilt archives are byte-identical."
                 Foreground="{DynamicResource ForegroundSecondary}" FontSize="{DynamicResource FontSizeCaption}"
                 TextWrapping="Wrap" Margin="0,0,0,2" />
      <!-- Same three links as the Advanced tab header (WCAG 3.2.4: identical labels + URLs, opened via
           OnResourceLinkClick in code-behind; the FTP-originals archive contains only the Windows
           binaries). The lead-in names its antecedent explicitly for the beginner audience. -->
      <WrapPanel Margin="0,0,0,4">
        <TextBlock Text="Download WinRAR versions from:"
                   Foreground="{DynamicResource ForegroundSecondary}" FontSize="{DynamicResource FontSizeCaption}"
                   VerticalAlignment="Center" Margin="0,0,4,0" />
        <Button Classes="link" Content="Extracted files for Windows (ready to use)"
                Tag="https://drive.google.com/file/d/1of053kS2Wxk-foHN_ALRu-u6Tcck58yn/view?usp=drive_link"
                Click="OnResourceLinkClick"
                FontSize="{DynamicResource FontSizeCaption}" VerticalAlignment="Center" />
        <TextBlock Text=","
                   Foreground="{DynamicResource ForegroundSecondary}" FontSize="{DynamicResource FontSizeCaption}"
                   VerticalAlignment="Center" Margin="0,0,4,0" />
        <Button Classes="link" Content="Extracted files for Linux (ready to use)"
                Tag="https://drive.google.com/file/d/1TcpA7RXoTUEr3pHZ8-4YTcQFRGZYP7v_/view?usp=drive_link"
                Click="OnResourceLinkClick"
                FontSize="{DynamicResource FontSizeCaption}" VerticalAlignment="Center" Margin="0,0,4,0" />
        <TextBlock Text="or"
                   Foreground="{DynamicResource ForegroundSecondary}" FontSize="{DynamicResource FontSizeCaption}"
                   VerticalAlignment="Center" Margin="0,0,4,0" />
        <Button Classes="link" Content="Original files from RAR FTP (Windows)"
                Tag="https://drive.google.com/file/d/1hvgzSY6YH_ZS3cpy7bHcw2zpjiwuP_Xi/view?usp=drive_link"
                Click="OnResourceLinkClick"
                FontSize="{DynamicResource FontSizeCaption}" VerticalAlignment="Center" />
      </WrapPanel>
      <DockPanel Margin="0,0,0,2" KeyboardNavigation.TabNavigation="Local">
        <Button DockPanel.Dock="Right" TabIndex="1" Content="Browse" Command="{Binding BrowseWinRARCommand}"
                AutomationProperties.Name="Browse for WinRAR versions folder"
                Classes="ghost" Margin="4,0,0,0" MinWidth="75" />
        <TextBox Text="{Binding WinRARPath}" TabIndex="0" AutomationProperties.LabeledBy="{Binding #WinRarFolderLabel}" />
      </DockPanel>
      <controls:FieldStatusLine Status="{Binding WinRARStatus}" />

      <TextBlock x:Name="ReleaseFilesLabel" Text="Extracted release files" FontWeight="SemiBold" Margin="0,12,0,2" />
      <TextBlock Text="The folder with the release's already-extracted files — the originals that were inside the .rar archives (the movie, sample, subtitles, …)."
                 Foreground="{DynamicResource ForegroundSecondary}" FontSize="{DynamicResource FontSizeCaption}"
                 TextWrapping="Wrap" Margin="0,0,0,2" />
      <DockPanel Margin="0,0,0,2" KeyboardNavigation.TabNavigation="Local">
        <Button DockPanel.Dock="Right" TabIndex="1" Content="Browse" Command="{Binding BrowseReleaseCommand}"
                AutomationProperties.Name="Browse for extracted release files"
                Classes="ghost" Margin="4,0,0,0" MinWidth="75" />
        <TextBox Text="{Binding ReleasePath}" TabIndex="0" AutomationProperties.LabeledBy="{Binding #ReleaseFilesLabel}" />
      </DockPanel>
      <controls:FieldStatusLine Status="{Binding ReleaseStatus}" />

      <TextBlock x:Name="OutputFolderLabel" Text="Output folder" FontWeight="SemiBold" Margin="0,12,0,2" />
      <TextBlock Text="Where the rebuilt .rar / .r00… volumes are written. Pick an empty folder so nothing is overwritten."
                 Foreground="{DynamicResource ForegroundSecondary}" FontSize="{DynamicResource FontSizeCaption}"
                 TextWrapping="Wrap" Margin="0,0,0,2" />
      <DockPanel Margin="0,0,0,2" KeyboardNavigation.TabNavigation="Local">
        <Button DockPanel.Dock="Right" TabIndex="1" Content="Browse" Command="{Binding BrowseOutputCommand}"
                AutomationProperties.Name="Browse for output folder"
                Classes="ghost" Margin="4,0,0,0" MinWidth="75" />
        <TextBox Text="{Binding OutputPath}" TabIndex="0" AutomationProperties.LabeledBy="{Binding #OutputFolderLabel}" />
      </DockPanel>
      <controls:FieldStatusLine Status="{Binding OutputStatus}" />

      <TextBlock x:Name="VerificationFileLabel" Text="Verification file (.sfv or .sha1)" FontWeight="SemiBold" Margin="0,12,0,2" />
      <TextBlock Text="Auto-filled from the SRR when it includes one — otherwise pick the release's .sfv. The rebuilt archives are checked against it to confirm they came out identical."
                 Foreground="{DynamicResource ForegroundSecondary}"
                 FontSize="{DynamicResource FontSizeCaption}"
                 TextWrapping="Wrap" Margin="0,0,0,2" />
      <DockPanel Margin="0,0,0,2" KeyboardNavigation.TabNavigation="Local">
        <Button DockPanel.Dock="Right" TabIndex="1" Content="Browse" Command="{Binding BrowseVerificationCommand}"
                AutomationProperties.Name="Browse for verification file"
                Classes="ghost" Margin="4,0,0,0" MinWidth="75" />
        <TextBox Text="{Binding VerificationPath}" TabIndex="0" AutomationProperties.LabeledBy="{Binding #VerificationFileLabel}" />
      </DockPanel>
      <controls:FieldStatusLine Status="{Binding VerifyStatus}" />

      <controls:FieldStatusLine Status="{Binding ArchiveSetStatus}" />

      <CheckBox Content="Recreate the whole release"
                IsChecked="{Binding CompleteAllVolumes}" Margin="0,14,0,2" />
      <TextBlock Text="When a match is found, let WinRAR finish writing every .rar volume of the release instead of stopping after the first one. Takes longer but gives you the complete archive set."
                 Foreground="{DynamicResource ForegroundSecondary}" FontSize="{DynamicResource FontSizeCaption}"
                 TextWrapping="Wrap" Margin="20,0,0,0" />

      <CheckBox Content="Rename to the release's file names"
                IsChecked="{Binding RenameToReleaseNames}"
                IsEnabled="{Binding IsRenameEnabled}" Margin="0,10,0,2" />
      <TextBlock Text="Name the rebuilt archives exactly like the original release (taken from the SRR, or the verification .sfv) instead of technical names like '420-m3-md4096k.rar'."
                 Foreground="{DynamicResource ForegroundSecondary}" FontSize="{DynamicResource FontSizeCaption}"
                 TextWrapping="Wrap" Margin="20,0,0,0" />
      </StackPanel>
    </ScrollViewer>

    <!-- Step 2: run + progress (started by the "Start" button on the previous step) -->
    <DockPanel IsVisible="{Binding $parent[Window].DataContext.CurrentStepIndex,
                                   Converter={StaticResource IndexEqualsConverter}, ConverterParameter=2}">
      <StackPanel DockPanel.Dock="Top">
        <ProgressBar Value="{Binding ProgressPercent}" Maximum="100" Height="6"
                     IsVisible="{Binding ShowProgress}" />
        <TextBlock Text="{Binding ProgressMessage}"
                   Foreground="{DynamicResource ForegroundSecondary}"
                   FontSize="{DynamicResource FontSizeCaption}"
                   Margin="0,4,0,0"
                   IsVisible="{Binding ShowProgress}" />
        <Button Content="Stop" Command="{Binding StopCommand}"
                Classes="cancel" Padding="14,4" Margin="0,8,0,0"
                HorizontalAlignment="Left"
                IsVisible="{Binding IsRunning}" />
        <Button Content="Open output folder" Command="{Binding OpenOutputFolderCommand}"
                Classes="primary" Padding="14,4" Margin="0,8,0,0"
                HorizontalAlignment="Left"
                IsVisible="{Binding LastRunSucceeded}" />
        <!-- Log header with a Save-log action so the run output can be kept (incl. the [P2] failure
             lines). Mirrors the sibling operation views and the Create-SRR wizard (WCAG 3.2.4
             Consistent Identification): Content is the accessible name; always-enabled — an empty
             press announces "nothing to save" via the live status line. The header also names the
             log list below via LabeledBy (4.1.2). -->
        <DockPanel Margin="0,12,0,2">
          <Button DockPanel.Dock="Right" Content="Save log..."
                  Command="{Binding SaveLogCommand}"
                  Classes="ghost" Padding="8,2" />
          <TextBlock x:Name="DetailsHeader" DockPanel.Dock="Left" Text="Details" FontWeight="SemiBold" VerticalAlignment="Center" />
          <!-- Live save-outcome line (4.1.3): always in the tree — visibility-toggling would race
               the announcement; empty text renders nothing. No AutomationProperties.Name: the
               announced name IS the text. -->
          <TextBlock x:Name="SaveLogStatus" Text="{Binding SaveLogAnnouncement}"
                     AutomationProperties.LiveSetting="Polite"
                     Foreground="{DynamicResource ForegroundSecondary}" FontSize="{DynamicResource FontSizeCaption}"
                     TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="8,0" />
        </DockPanel>
      </StackPanel>
      <!-- The merged chronological log (same LogEntries the Advanced tab shows): the System-only
           TextBox hid the engine's [P2] failure lines; the shared logList ListBox virtualizes and
           auto-sticks to the newest line while the reader is at the bottom. -->
      <Border BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="1"
              Background="{DynamicResource SystemControlBackgroundAltHighBrush}" MinHeight="120">
        <ListBox ItemsSource="{Binding LogEntries}" Classes="logList"
                 AutomationProperties.LabeledBy="{Binding #DetailsHeader}"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 FontFamily="{DynamicResource MonoFontFamily}" FontSize="{DynamicResource MonoFontSize}" />
      </Border>
    </DockPanel>

  </Grid>
</UserControl>
